# Sanity project — create one free at https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: pin the API version (defaults to today's date in code if unset)
NEXT_PUBLIC_SANITY_API_VERSION=2025-01-01
